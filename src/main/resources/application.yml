spring:
  datasource:
    url: jdbc:h2:mem:gameDbTest
    driver-class-name: org.h2.Driver
    username: sa
    password: groot
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop